# 工科数学分析 I 课堂笔记：2026-09-21

## 来源

- 章节：1.1 实数集及其完备性
- 来源状态：目前仅收到课程名称与章节标题
- 待补：课件、教材页码、课堂笔记或录音

以下内容按工科数学分析的标准知识框架整理，待课堂材料到手后核对。

## 一、实数集的结构

- 实数集 `R` 是完备的有序域。
- 有序域表示四则运算与大小关系相容。
- 完备性的核心表现是确界原理。
- 有理数集 `Q` 是稠密的有序域，但不完备。

## 二、上下界

若对任意 `x in E` 都有 `x <= M`，则 `M` 是 `E` 的上界。

若对任意 `x in E` 都有 `m <= x`，则 `m` 是 `E` 的下界。

上下界通常不唯一。有界集合不一定有最大元或最小元。

## 三、上确界与下确界

`s = sup E` 表示 `s` 是 `E` 的最小上界。

`t = inf E` 表示 `t` 是 `E` 的最大下界。

等价刻画：

- `s = sup E` 当且仅当对任意 `epsilon > 0`，存在 `x in E` 使 `x > s - epsilon`。
- `t = inf E` 当且仅当对任意 `epsilon > 0`，存在 `x in E` 使 `x < t + epsilon`。

确界不一定属于集合。若上确界属于集合，则它也是最大元；若下确界属于集合，则它也是最小元。

## 四、确界原理

- 任一非空有上界的实数子集必有上确界。
- 任一非空有下界的实数子集必有下确界。

确界原理是实数完备性的基本刻画，也是极限理论的重要基础。

## 五、Archimedean 性质

任取正实数 `a`，存在正整数 `n` 使 `n > a`。

等价形式：任取 `epsilon > 0`，存在正整数 `n` 使 `1/n < epsilon`。

典型用途是证明 `inf {1/n | n 是正整数} = 0`。

## 六、稠密性

- 任意两个不等实数之间都有有理数。
- 任意两个不等实数之间都有无理数。

稠密性只表示集合在任意小的非空开区间中都出现，不等同于集合完备。

## 七、绝对值与邻域

当 `delta > 0` 时：

`|x - a| < delta`

等价于：

`a - delta < x < a + delta`

区间 `(a - delta, a + delta)` 是点 `a` 的 `delta` 邻域。

## 八、例题

1. `E = (0, 1)` 时，`sup E = 1`，`inf E = 0`，无最大值、最小值。
2. `E = {1 - 1/n | n 是正整数}` 时，`sup E = 1` 且不属于 `E`，`inf E = 0` 且属于 `E`。
3. 若 `-E = {-x | x in E}`，则 `sup(-E) = -inf E`，`inf(-E) = -sup E`。

## 九、易错点

- 上界不一定是上确界，上确界是最小的上界。
- 上确界不一定属于原集合。
- 有界集合不一定有最大元或最小元。
- 证明确界时既要证明它是界，也要排除更紧的界。
- 使用稠密性时，区间必须非空且端点不相等。

## 待课堂材料核对

- 教师采用“确界原理”还是“最小上界性”等术语。
- 是否补充区间套定理、单调有界定理或实数公理化定义。
- 邻域符号、开闭区间与绝对值性质的课堂约定。
